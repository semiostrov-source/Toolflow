## Warehouses components

This directory will contain React components dedicated to the warehouses feature, such as warehouse lists, detail views, maps or layouts, and status indicators. Components here should focus on presenting and interacting with warehouse-related data within this feature.

Generic UI elements that are reused across multiple features should live in a shared/components area instead, so that the warehouses module remains focused on warehouse-specific presentation concerns.
