## Inventory types

This directory will contain TypeScript types and interfaces that describe the inventory domain: items, stock levels, movements, and any related value objects. These types should represent the core data structures used by the inventory feature, independent of specific API or storage details.

Keep inventory-specific types here and reuse cross-cutting or shared types from a shared/types area when appropriate to maintain clear boundaries between features.
