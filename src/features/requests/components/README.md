## Requests components

This directory will contain React components specific to the requests feature, such as request creation forms, lists, detail views, and status indicators. Components here should focus on presenting and managing request-related interactions within this feature.

UI elements that are reused by multiple features should live in a shared/components area, so that the requests module stays focused on request-centric presentation and behavior.
